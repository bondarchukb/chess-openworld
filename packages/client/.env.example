# Point the client at your deployed game server (WebSocket URL).
# Local dev doesn't need this — `npm run dev` proxies /ws to localhost:8080.
# In production (e.g. Vercel), set this to your server's wss:// URL:
VITE_WS_URL=wss://your-server.onrender.com
