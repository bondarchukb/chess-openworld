{
  "name": "@chess-openworld/client",
  "version": "0.0.0",
  "description": "Isometric web client for the open-world chess MMO.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@chess-openworld/engine": "*",
    "@chess-openworld/protocol": "*",
    "pixi.js": "^8.5.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
